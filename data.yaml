path: dataset  # dataset root dir (folder yang baru saja di-rename)
train: train/images  # train images (relative to 'path')
val: valid/images  # val images (relative to 'path')
test: test/images  # opsional

# Classes
names:
  0: panel
